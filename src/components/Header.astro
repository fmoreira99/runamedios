<nav class="header" id="site-header">
  <div class="header-bar">
    <div class="logo fade-in-logo">
      <a href={import.meta.env.BASE_URL}>
        <img
          width="35"
          height="30"
          style="margin-top: 15px;"
          src="../../public/favicon.svg"
          alt="RunaMedia logo"
        />
      </a>
      
    </div>

    <button
      class="menu-toggle"
      aria-controls="primary-menu"
      aria-expanded="false"
      aria-label="Abrir menú principal"
    >
      <svg xmlns="http://www.w3.org/2000/svg" x="0px" y="0px" width="24" height="24" viewBox="0,0,256,256">
<g fill="#ffffff" fill-rule="nonzero" stroke="none" stroke-width="1" stroke-linecap="butt" stroke-linejoin="miter" stroke-miterlimit="10" stroke-dasharray="" stroke-dashoffset="0" font-family="none" font-weight="none" font-size="none" text-anchor="none" style="mix-blend-mode: normal"><g transform="scale(10.66667,10.66667)"><path d="M5,7h2h12c1.1,0 2,-0.9 2,-2c0,-1.1 -0.9,-2 -2,-2h-12h-2c-1.1,0 -2,0.9 -2,2c0,1.1 0.9,2 2,2z"></path><path d="M19,10h-3h-11c-1.1,0 -2,0.9 -2,2c0,1.1 0.9,2 2,2h11h3c1.1,0 2,-0.9 2,-2c0,-1.1 -0.9,-2 -2,-2z"></path><path d="M19,17h-6h-8c-1.1,0 -2,0.9 -2,2c0,1.1 0.9,2 2,2h8h6c1.1,0 2,-0.9 2,-2c0,-1.1 -0.9,-2 -2,-2z"></path></g></g>
</svg>
    </button>

    <div class="header-ads"></div>

    <ul id="primary-menu" class="menu">
      <li class="menu-item slide-in" style="--delay: 0.1s">
        <a href={import.meta.env.BASE_URL}>Inicio</a>
      </li>
      <li class="menu-item slide-in" style="--delay: 0.2s">
        <a href={import.meta.env.BASE_URL + "noticias"}>Noticias</a>
      </li>
      <li class="menu-item slide-in" style="--delay: 0.3s">
        <a href="#recomendadas">Memes</a>
      </li>
    
      <li class="menu-item slide-in" style="--delay: 0.7s">
        <a href={import.meta.env.BASE_URL + "contacto"}>Contacto</a>
      </li>
    </ul>
  </div>
</nav>

<style>
  .header {
    width: 100%;
    padding: 12px 20px;
    background: #0c0f12;
    box-sizing: border-box;
    color: #ccd0d4;
    border-bottom: 1px solid rgba(255, 255, 255, 0.06);
  }
  .header-bar {
    display: flex;
    align-items: center;
    gap: 20px;
    width: 100%;
  }
  .logo {
    display: flex;
    align-items: center;
    gap: 12px;
  }
  .logo a {
    font-weight: 700;
    text-decoration: none;
    color: #4cf2d6;
  }

  .header-ads {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-left: auto;
  }
  .ad-468x60,
  .ad-320x50 {
    border: 1px dashed rgba(255, 255, 255, 0.35);
    border-radius: 10px;
    background: rgba(0, 0, 0, 0.15);
    color: #e8ecef;
    font-weight: 700;
    letter-spacing: 0.02em;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .ad-468x60 {
    width: 468px;
    height: 60px;
  }
  .ad-320x50 {
    width: 320px;
    height: 50px;
    display: none;
  }

  .menu {
    list-style: none;
    display: flex;
    gap: 20px;
    margin: 12px 0 0;
    padding: 0;
    flex-wrap: wrap;
  }
  .menu a {
    text-decoration: none;
    color: #ccd0d4;
    font-weight: 500;
  }
  .menu a:hover {
    color: #4cf2d6;
  }

  .menu-toggle {
    display: none;
    background: #01402e;
    color: #fff;
    border: none;
    padding: 8px 12px;
    border-radius: 6px;
    font-size: 18px;
    cursor: pointer;
  }
  .menu-toggle .bar {
    display: block;
    width: 22px;
    height: 2px;
    background: #fff;
    margin: 4px 0;
    border-radius: 2px;
  }

  /* Animaciones original */
  @keyframes fadeIn {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }
  @keyframes slideInRight {
    from {
      opacity: 0;
      transform: translateX(30px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }
  .fade-in-logo {
    animation: fadeIn 0.8s ease-out forwards;
  }
  .slide-in {
    opacity: 0;
    animation: slideInRight 0.6s ease-out forwards;
    animation-delay: var(--delay);
  }

  @media (max-width: 1024px) {
    .header-ads {
      flex-wrap: wrap;
      justify-content: flex-end;
    }
  }

  @media (max-width: 780px) {
    .header-bar {
      flex-wrap: wrap;
    }
    .menu-toggle {
      display: inline-flex;
      margin-left: auto;
    }
    .header-ads {
      width: 100%;
      justify-content: center;
      order: 3;
    }
    .ad-468x60 {
      display: none;
    }
    .ad-320x50 {
      display: flex;
    }

    .menu {
      position: fixed;
      left: 0;
      right: 0;
      top: 60px;
      background: rgba(12, 15, 18, 0.98);
      padding: 24px 20px;
      margin: 0;
      flex-direction: column;
      gap: 16px;
      border-top: 1px solid rgba(255, 255, 255, 0.08);
      display: none;
      z-index: 1000;
    }
    .menu.open {
      display: flex;
    }
  }

  :global(body.no-scroll) {
    overflow: hidden;
  }
</style>

<script is:inline>
  (function () {
    const header = document.getElementById("site-header");
    if (!header) return;
    const btn = header.querySelector(".menu-toggle");
    const menu = header.querySelector(".menu");
    if (!btn || !menu) return;

    const closeMenu = () => {
      menu.classList.remove("open");
      btn.setAttribute("aria-expanded", "false");
      document.body.classList.remove("no-scroll");
    };

    btn.addEventListener("click", () => {
      const isOpen = menu.classList.toggle("open");
      btn.setAttribute("aria-expanded", isOpen ? "true" : "false");
      document.body.classList.toggle("no-scroll", isOpen);
    });

    menu.addEventListener("click", (event) => {
      const target = event.target;
      if (target instanceof HTMLElement && target.closest("a")) {
        closeMenu();
      }
    });

    window.addEventListener("resize", () => {
      if (window.innerWidth > 780) {
        closeMenu();
      }
    });
  })();
</script>
