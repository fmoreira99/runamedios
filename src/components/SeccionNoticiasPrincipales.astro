<section class="principales contenedor"  id="principales">
  <h2 class="titulo-seccion princ-verde reveal-up" style="--delay: .05s; color: #02A073;">Noticias principales</h2>
  <div class="grid-3">
    <a class="card reveal-up" style="--delay: .12s" href={import.meta.env.BASE_URL + 'noticias/noticia-9999'}>
      <img src="https://res.cloudinary.com/durxpegdm/image/upload/v1762540572/noticias/gu_a_completa_c_mo_ganar_dinero_por_internet_2_modxom.jpg" alt="Noticia destacada 1" />
      <h3>Como ganar dinero por internet, sin invesión</h3>
      <p>Descubre métodos probados y 100% gratuitos para generar ingresos desde la comodidad de tu casa. En este artículo, aprenderás paso a paso las estrategias más efectivas para ganar dinero por internet sin necesidad de inversión inicial.</p>
    </a>
    <a class="card reveal-up" style="--delay: .2s" href={import.meta.env.BASE_URL + 'noticias/noticia-9998'}>
      <img src="https://res.cloudinary.com/durxpegdm/image/upload/v1762541707/noticias/contexto_de_descuento_y_promociones_una_imagen_wdoe7i.jpg" alt="Noticia destacada 2" />
      <h3>Las mejores 9 webs y apps para encontrar ofertas o chollos de todo tipo</h3>
      <p>Descubre 9 plataformas confiables para encontrar ofertas, cupones, promociones y productos al mejor precio en Internet, desde viajes hasta tecnología</p>
    </a>
    <a class="card reveal-up" style="--delay: .2s" href={import.meta.env.BASE_URL + 'noticias/noticia-9997'}>
      <img src="https://res.cloudinary.com/durxpegdm/image/upload/v1762544331/noticias/ChatGPT_Image_7_nov_2025_14_38_44_n2hvi6.png" alt="Noticia destacada 3" />
      <h3>Guía completa para monetizar en Roblox en 2025-2026</h3>
      <p>Ve más allá del titular. En esta sección, desmantelamos la superficie de los temas más comentados para ofrecerte un análisis profundo y riguroso. Aquí encontrarás la explicación completa y fundamentada detrás de los hechos que están marcando la conversación.</p>
    </a>
  </div>
  <div class="acciones">
    <a class="btn secundario color-verde buttonCuti reveal-up" style="--delay: .36s;background:#E00B00; color: #fff; border:0px" href={import.meta.env.BASE_URL + 'noticias'}>Ver todas las noticias...</a>
  </div>
</section>


<style>
  /* Animación scroll para "Noticias principales" */
  .reveal-up {
    opacity: 0;
    transform: translateY(30px);
    filter: blur(6px);
    transition: transform .6s ease, opacity .6s ease, filter .6s ease;
    transition-delay: var(--delay, 0s);
    will-change: transform, opacity, filter;
  }
  .in-view.reveal-up {
    opacity: 1;
    transform: translateY(0);
    filter: blur(0);
  }
</style>


<style>
  .hero { padding: 0px 20px; background: linear-gradient(135deg, #0c0f12 10%, #10161c 90%); }
  .hero-inner { display: grid; grid-template-columns: 1fr 360px; align-items: center; gap: 36px; }
  @media (max-width: 900px) { .hero-inner { grid-template-columns: 1fr; } }
  .text h1 {
    font-size: 48px;
    color: #ffffff;
    margin: 0;
    font-weight: 800;
  }
  .text h2 {
    margin-top: 12px;
    color: #01402E;
    font-size: 22px;
    font-weight: 400;
  }
  .btn {
    margin-top: 30px;
    display: inline-block;
    padding: 12px 26px;
    border-radius: 6px;
    background: #4cf2d6;
    color: #0c0f12;
    font-weight: 600;
    text-decoration: none;
  }
  .btn:hover {
    background: #36bba5;
  }

  /* Layout utilitario */
  .contenedor { max-width: 1100px; margin: 0 auto; padding: 40px 20px; }
  .titulo-seccion { color: #4cf2d6; margin: 0 0 18px; font-size: 28px; }
  .grid-3 { display: grid; grid-template-columns: repeat(3, 1fr); gap: 22px; }
  .grid-4 { display: grid; grid-template-columns: repeat(4, 1fr); gap: 18px; }
  @media (max-width: 900px) { .grid-3 { grid-template-columns: 1fr; } .grid-4 { grid-template-columns: repeat(2, 1fr); } }

  /* Cards principales */
  .card { background: #01402E; border: 1px solid rgba(255,255,255,0.06); border-radius: 10px; padding: 14px; text-decoration: none; color: #ccd0d4; transition: transform .18s ease, background .18s ease; display: block; }
  .card:hover { background: #01402E; transform: translateY(-2px); }
  .card img { width: 100%; height: 180px; object-fit: cover; border-radius: 8px; background: #0c0f12; }
  .card h3 { margin: 12px 0 6px; font-size: 18px; color: #e8ecef; }
  .card p { margin: 0; opacity: 0.75; font-size: 14px; }
  .acciones { margin-top: 18px; text-align: right; }
  .btn.secundario { background: transparent; color: #4cf2d6; border: 1px solid #2a3b46; }
  .btn.secundario:hover { background: #142026; }

  /* Recomendadas */
  .mini { display: flex; gap: 12px; align-items: center; background: #0f141a; border: 1px solid rgba(255,255,255,0.06); border-radius: 10px; padding: 10px; color: #ccd0d4; text-decoration: none; transition: background .18s ease; }
  .mini {
    transition: background 0.3s ease, transform 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
    transform-origin: center;
  }
  .mini:hover {
    background: #131b23;
    transform: scale(1.12);
    z-index: 1;
  }
  .mini img { width: 44px; height: 44px; object-fit: cover; border-radius: 6px; background: #0c0f12; }
  .mini .tag { display: inline-block; font-size: 12px; font-weight: 600; color: #ffffff; background: #00e08aa9; border-radius: 999px; padding: 3px 10px; margin-bottom: 6px; }
  .mini p { margin: 0; font-size: 13px; opacity: 0.8; }
  /* Canales */
  .canales-grid { gap: 18px; }
  .canal { background: #0f141a; border: 1px solid rgba(255,255,255,0.02); border-radius: 10px; padding: 16px; }
  .canal h3 { margin: 0 0 6px; color: #e8ecef; font-size: 18px; }
  .canal p { margin: 0 0 6px; font-size: 14px; opacity: 0.85; }
  /* Valores */
  .lista-valores { margin: 0; padding-left: 18px; }
  .lista-valores li { margin: 6px 0; }
  .llamado p { margin: 0 0 12px; }
</style>

<style>
  /* Carrusel con efecto glow (9:16) */
  .carousel { 
    width: 360px; 
    margin: 0 auto;
    position: relative;
    z-index: 1;
  }
  @media (max-width: 900px) { .carousel { width: 260px; } }

  /* EFECTO GLOW ANIMADO - Debe ir en el carousel, no en slides */
  .carousel::before {
    content: "";
    position: absolute;
    top: 30px;
    left: 50%;
    transform: translateX(-50%) scale(0.8);
    z-index: -1;
    height: 100%;
    width: 100%;
    filter: blur(10px);
    -webkit-filter: blur(100px);
    background: linear-gradient(270deg, #01402E, #038239);
    background-size: 200% 200%;
    animation: animateGlow 10s ease infinite;
    border-radius: 12px;
  }

  @keyframes animateGlow {
    0% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
    100% { background-position: 0% 50%; }
  }
  
  .slides { 
    position: relative; 
    aspect-ratio: 9 / 16; 
    border-radius: 12px; 
    overflow: hidden; 
    background: #0c0f12; 
    border: 1px solid rgba(255,255,255,0.06);
  }

  /* Mostrar/ocultar cada slide por wrapper */
  .slide-wrapper { 
    position: absolute; 
    inset: 0; 
    width: 100%; 
    height: 100%; 
    opacity: 0; 
    transition: opacity .35s ease; 
  }
  .slide-wrapper.active { opacity: 1; }

  .slide { 
    position: absolute; 
    inset: 0; 
    width: 100%; 
    height: 100%; 
    object-fit: cover; 
  }
  
  .dots { 
    display: flex; 
    gap: 8px; 
    justify-content: center; 
    margin-top: 10px; 
  }
  .dot { 
    width: 8px; 
    height: 8px; 
    border-radius: 999px; 
    border: none; 
    background: #2a3b46; 
    cursor: pointer; 
    padding: 0; 
  }
  .dot.active { background: #4cf2d6; }

  /* Overlay con título y descripción */
  .slide-overlay {
    position: absolute;
    inset: auto 0 0 0;
    padding: 12px 14px;
    background: linear-gradient(180deg, rgba(12,15,18,0) 0%, rgba(12,15,18,0.75) 40%, rgba(12,15,18,0.95) 100%);
    color: #e8ecef;
  }
  .slide-tag {
    display: inline-block;
    font-size: 11px;
    font-weight: 700;
    text-transform: lowercase;
    color: #0c0f12;
    background: #4cf2d6;
    border-radius: 999px;
    padding: 4px 10px;
    margin-bottom: 6px;
  }
  .slide-title { margin: 6px 0 4px; font-size: 16px; font-weight: 700; }
  .slide-desc { margin: 0; font-size: 13px; opacity: 0.85; }
</style>